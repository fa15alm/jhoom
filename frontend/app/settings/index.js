import PlaceholderScreen from "../../components/ui/PlaceholderScreen";

export default function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      text="Profile settings will live here once we build out the dashboard flow."
    />
  );
}
