# Shared

Reusable frontend building blocks live here.

- `ui/` - shared components used across route screens.
- `hooks/` - reusable hooks such as responsive mobile-frame sizing.
- `types/` - shared domain type placeholders for backend data shapes.
